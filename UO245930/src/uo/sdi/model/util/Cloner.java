package uo.sdi.model.util;

import uo.sdi.model.*;

public class Cloner {

	// public static Task clone(Task t) {
	// Task t = new Task(t.getName())
	// return new Task()
	// .setCategory( t.getCategory().getId() )
	// .setComments( t.getComments() )
	// .setCreated( t.getCreated() )
	// .setFinished( t.getFinished() )
	// .setId( t.getId() )
	// .setPlanned( t.getPlanned() )
	// .setTitle( t.getTitle() )
	// .setUser( t.getUserId() );
	// }
	//
	// public static Category clone(Category c) {
	// return new Category()
	// .setName( c.getName() )
	// .setUserId( c.getUserId() );
	// }
}